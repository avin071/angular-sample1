name: Angular1js CI/CD

on:
  push:
    branches:
      - 'feature/angular'

jobs:
  build:
    name: Build 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: npm install and npm run build
        run: |
          npm install
          
